/**
 * Native splash + startup shell — matches Night Calm `Dawn.background.primary`.
 * Full-screen asset: `assets/images/splash-full.png` (regenerate via `npm run generate:splash`).
 */
export const SPLASH_BACKGROUND = '#0E223D';
